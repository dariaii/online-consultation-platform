@model string
@{
    ViewData["Title"] = @Localizer["Account_NotAuthorized"];
    Layout = "_LayoutAnonymous";
}

<form class="form-wrap">
    <div class="logo-box">
        <img class="login-inedu-logo" src="~/images/inedu-logo.png" alt="InEdu Logo" />
    </div>

    <p>@Localizer["Account_UserDeactivatedMessage", Model]</p>

    <a asp-controller="Account" asp-action="@nameof(AccountController.Login)" class="btn login-btn">@Localizer["Account_Login"]</a>
</form>